<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Purchases</title>
    <!-- IMPORTANT: Link to your external booking.css file for all custom styling. -->
    <link rel="stylesheet" href="/css/booking.css">
</head>
<body>
    <!-- Navbar (styled by booking.css, links to correct Express routes) -->
    <nav class="w-full flex justify-center">
        <div class="navbar">
            <div class="logo">
                <h2>Car Showroom</h2>
            </div>
            <ul>
           <li><a href="/">Home</a></li>
                <li><a href="/carllisting" class="active">Car Listings</a></li>
                <li><a href="/Dashboard">Dashboard</a></li>
                <li><a href="/Contact">Contact</a></li>
                <li><a href="/login">Sign Up/Login</a></li>
                <li><a href="/api/bookingsales/view-my-purchases">My Purchases</a></li>
               
            </ul>
            <div class="cart-container">
                <a href="/cart" class="cart-text-button">
                    Cart <span class="cart-count">0</span>
                </a>
            </div>
        </div>
    </nav>

    <!-- This is the CORRECT location for your standalone Back Button. -->
    <a href="javascript:history.back()" class="back-button">⬅ Back</a>

    <div class="container">
        <h1>My Purchase History</h1>

        <div id="message" class="message info" style="display: none;">
            <!-- Message will be displayed here if no purchases -->
        </div>

        <% if (purchases && purchases.length > 0) { %>
            <div class="overflow-x-auto">
                <table class="min-w-full">
                    <thead>
                        <tr>
                            <th>Car Name</th>
                            <th>Model</th>
                            <th>Quantity</th>
                            <th>Unit Price</th>
                            <th>Total Price</th>
                            <th>Purchase Date</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody id="purchasesTableBody">
                        <% purchases.forEach(purchase => { %>
                            <tr>
                                <td><%= purchase.carId ? purchase.carId.name : 'N/A' %></td>
                                <td><%= purchase.carId ? purchase.carId.model : 'N/A' %></td>
                                <td><%= purchase.quantity %></td>
                                <td>$<%= purchase.unitPrice ? purchase.unitPrice.toFixed(2) : '0.00' %></td>
                                <td>$<%= purchase.totalPrice ? purchase.totalPrice.toFixed(2) : '0.00' %></td>
                                <td><%= new Date(purchase.purchaseDate).toLocaleDateString() %></td>
                                <td><%= purchase.status %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <p class="text-center text-white text-lg py-10">You have no purchase history yet.</p> 
        <% } %>
    </div>
    
</body>
</html>