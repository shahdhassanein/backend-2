<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Car</title>
    <link rel="stylesheet" href="/styles/updatecar.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div class="container">
        <div class="form-card">
            <h2><i class="fas fa-edit"></i> Update Car Details</h2>

            <!-- Form will be submitted via JavaScript (AJAX) -->
            <form id="updateCarForm" method="POST">
                <!-- Use _id if MongoDB is being used -->
                <input type="hidden" id="carId" name="carId" value="<%= car._id %>">

                <div class="form-group">
                    <label for="name">Car Name:</label>
                    <input type="text" id="name" name="name" value="<%= car.name %>" required maxlength="100">
                </div>

                <div class="form-group">
                    <label for="brand">Brand:</label>
                    <input type="text" id="brand" name="brand" value="<%= car.brand %>" required maxlength="100">
                </div>

                <div class="form-group">
                    <label for="category">Category:</label>
                    <input type="text" id="category" name="category" value="<%= car.category %>" required maxlength="50">
                </div>

                <div class="form-group">
                    <label for="price">Price:</label>
                    <input type="number" id="price" name="price" value="<%= car.price %>" required min="0" step="0.01">
                </div>

                <div class="form-group">
                    <label for="stock">Stock:</label>
                    <input type="number" id="stock" name="stock" value="<%= car.stock %>" required min="0">
                </div>

                <div class="form-group">
                    <label for="image">Image URL:</label>
                    <input type="url" id="image" name="image" value="<%= car.image %>" required>
                </div>

                <button type="submit" class="btn-submit">Update Car</button>
                <a href="/api/cars" class="btn-cancel">Cancel</a>
            </form>

            <!-- Message container for success or error messages -->
            <div id="message" class="message"></div>
        </div>
    </div>

    <script src="/js/updatecar.js"></script>
</body>
</html>
