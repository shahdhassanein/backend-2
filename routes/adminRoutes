// File: routes/adminRoutes.js

// 1. Import the express library
const express = require('express');

// 2. Create an express router object
const router = express.Router();


// --- ADD YOUR ADMIN-SPECIFIC ROUTES HERE ---
// For example:
// router.get('/dashboard-stats', getAdminStats);
// router.post('/products/add', protect, authorize('admin'), addProduct);
// router.delete('/users/:id', protect, authorize('admin'), deleteUser);


// 3. Export the router so app.js can use it
module.exports = router;